version: 1.0
    pipeline-when:
      equal:
        - << event.ref >>
        - refs/heads/main
    jobs:
      catalyst-deploy:
        steps:
          - npm install -g catalyst-cli
          - catalyst deploy appsail --name dashboard_app --build-path ./